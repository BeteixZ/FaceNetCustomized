from setuptools import find_packages, setup

setup(name='evolveface',
      version='0.0.1',
      description='Face detection and recognition.',
      packages=find_packages(),
      install_requires=[])
